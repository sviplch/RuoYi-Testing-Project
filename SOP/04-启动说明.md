# 04 — RuoYi 环境启动说明

> 环境已全部搭好。以后每次要练测试，照着这个启动即可。

---

## 一、系统组成

| 组件 | 地址 | 说明 |
|------|------|------|
| 前端 | http://localhost:80 | Vue2 + Element UI |
| 后端 | http://localhost:8080 | Spring Boot 2.5.15 |
| 数据库 | localhost:3306 / ry_vue | MySQL 8.0+（31 张表） |
| 缓存 | localhost:6379 | Redis |
| 登录账号 | admin / admin123 | - |

---

## 二、启动步骤（每次开机后）

### 前置：确认 MySQL 和 Redis 已在运行

```bash
# 这两个是 Windows 服务，开机自启，一般不用管。
# 如果不确定，检查一下（PowerShell 里用 Get-Service）：
Get-Service MySQL97, Redis

# 如果你在 Git Bash 里，则改用：
# sc query MySQL97 | grep STATE    # 应该显示 RUNNING
# sc query Redis | grep STATE      # 应该显示 RUNNING
```

如果 Redis 没启动，手动启动：
```bash
net start Redis
```

---

### 第 1 步：启动后端

开一个终端：

```bash
cd C:/projects/RuoYi-Vue
java -jar ruoyi-admin/target/ruoyi-admin.jar
```

看到 `Started RuoYiApplication` 和那个 ASCII 小图标，说明后端起来了。

> 注意：如果 `java` 命令提示找不到，说明终端是旧的没加载环境变量。重启终端即可；或者用完整路径：
> `"C:/Program Files/Eclipse Adoptium/jdk-8.0.502.7-hotspot/bin/java.exe" -jar ruoyi-admin/target/ruoyi-admin.jar`

---

### 第 2 步：启动前端

再开一个终端：

```bash
cd C:/projects/RuoYi-Vue2
npm run dev
```

看到 `App running at: http://localhost:80/`，前端就起来了。

---

### 第 3 步：浏览器访问

打开 **http://localhost** ，用 `admin / admin123` 登录。

---

## 三、完整启动命令速查

```bash
# 后端（终端1）
cd C:/projects/RuoYi-Vue && java -jar ruoyi-admin/target/ruoyi-admin.jar

# 前端（终端2）
cd C:/projects/RuoYi-Vue2 && npm run dev
```

---

## 四、停止项目

启动和停止是配套的，这里补上停止方式。

### 停止前端 / 后端

在跑 `npm run dev` 或 `java -jar` 的那个终端里，直接按 `Ctrl + C` 即可停止。

- 后端停止后：8080 端口释放，前端登录会转圈或提示验证码错误
- 前端停止后：浏览器访问 http://localhost 打不开

### 停止 MySQL / Redis 服务

这两个是 Windows 服务，开机自启，平时不用停。如果确实需要彻底停掉：

```bash
net stop Redis
net stop MySQL97
```

> 停了之后，下次启动项目前要先 `net start` 再开回来，否则后端起不来。

### 如果 Ctrl+C 没停干净（端口被占用）

偶尔 Ctrl+C 后进程没完全退出，下次启动报"端口被占用"。这样排查：

```bash
# 查谁占用了 8080 端口，最后一列是 PID
netstat -ano | Select-String 8080

# 强制结束该进程（把 <PID> 换成上面查到的数字）
taskkill /F /PID <PID>
```

> PowerShell / CMD 用单斜杠 `/F /PID`；Git Bash 里 taskkill 要用双斜杠 `//F //PID`（避免被当成路径）。

---

## 五、常见问题排查

| 现象 | 可能原因 | 解决 |
|------|----------|------|
| 后端启动报数据库连接失败 | MySQL 没启动，或密码不对 | 检查 `sc query MySQL97`，密码在 `application-druid.yml` 里 |
| 后端启动报 Redis 连接失败 | Redis 没启动 | `net start Redis` |
| 前端登录提示验证码错误 | 后端没启动 | 先启动后端再试 |
| 页面能开但登录转圈 | 前端连不上后端 | 后端是否在 8080 端口正常监听 |
| `java` / `mvn` 命令找不到 | 终端是旧会话 | 重开终端，环境变量已配置好 |

---

## 六、几个关键路径备忘

| 内容 | 路径 |
|------|------|
| 后端项目 | `C:\projects\RuoYi-Vue` |
| 前端项目 | `C:\projects\RuoYi-Vue2` |
| 后端配置文件（数据库连接） | `C:\projects\RuoYi-Vue\ruoyi-admin\src\main\resources\application-druid.yml` |
| 后端打包产物 | `C:\projects\RuoYi-Vue\ruoyi-admin\target\ruoyi-admin.jar` |
| 数据库 SQL 脚本 | `C:\projects\RuoYi-Vue\sql\` |
| Maven 安装目录 | `C:\tools\apache-maven-3.9.16` |

---

## 七、如果改了后端代码需要重新打包

```bash
cd C:/projects/RuoYi-Vue
mvn clean package -DskipTests
# 打包完再 java -jar 启动即可
```

> 练测试阶段一般不动后端代码，这步用不上，知道有这回事就行。
